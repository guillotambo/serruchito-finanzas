// Set de íconos del shell (sidebar/header): lucide-react, re-exportados acá
// bajo los mismos nombres que ya usaban los SVG custom, para no tocar los
// consumidores (Sidebar/Header/PriceRefreshStatus).
export {
  LayoutDashboard as IconDashboard,
  PieChart as IconPortfolio,
  TrendingUp as IconPerformance,
  ArrowLeftRight as IconMovements,
  Settings as IconSettings,
  ChevronLeft as IconChevronLeft,
  PanelLeft as IconPanelLeft,
  Menu as IconMenu,
  RefreshCw as IconRefresh,
  CandlestickChart as IconMarket,
} from "lucide-react";
